function uppercase(str) {
  return str.toUpperCase();
}

function fistLetter(str) {
  return str[0];
}

function join(str1, str2) {
  return str1 + str2
}

module.exports = {
  uppercase,
  fistLetter,
  join
}